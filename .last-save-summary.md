# ملخص الجلسة – Icon Fix + Social Reorder + Cafe Page Redesign + Upload Fixes

## أجندة العمل

### 1. إصلاح أيقونات `/ar/icon` و `/ar/apple-icon` 404
- **إنشاء `src/app/[locale]/icon.tsx`** و **`src/app/[locale]/apple-icon.tsx`**:
  - نفس محتوى الملفين الأصليين في `src/app/icon.tsx` و `src/app/apple-icon.tsx`
  - يعالج طلبات المتصفح `/[locale]/icon` و `/[locale]/apple-icon`
  - **بحاجة لرفع على Vercel عشان يشتغل Production**

### 2. إصلاح `products.steps.platform` MISSING_MESSAGE
- **السبب**: ملف `ar.json` / `en.json` اتحدث في آخر commit (إضافة `platform`, `stickerType`, `customization`, `theme`, `review`)
- **الحل**: إعادة تشغيل dev server - `next-intl` بيخبّئ الرسائل ديناميكياً و HMR مش بيحدثها تلقائياً

### 3. إصلاح Upload API 500 + تحسين معالجة الأخطاء
- **`src/app/api/upload/route.ts`**:
  - إزالة `fetch_format` (parameter غير صالح للـ `upload_stream`)
  - إضافة `uploadStream.on("error", reject)` لالتقاط أخطاء الـ stream
- **`src/components/admin/CafeForm.tsx`**:
  - تحسين `res.json()` — لو السيرفر رجع HTML بدل JSON، يقرأ `res.text()` عشان يظهر الخطأ مفهوماً بدل `"Unexpected token"`
- **`src/components/admin/ProductForm.tsx`**: نفس التحسين

### 4. إعادة ترتيب Social Links في `ProfileWrapper.tsx` (لكل البروفايلات)
تغيير ترتيب `buildButtons()` من:
```
Instagram → Facebook → TikTok → Snapchat → WhatsApp → Call
```
إلى:
```
Instagram → TikTok → Facebook → Snapchat → Call → WhatsApp
```
عشان الـ 2-column grid يبقى:
| Instagram | TikTok |
| Facebook | Snapchat |
| Call | WhatsApp |

### 5. redesign صفحة الكافيه (`src/app/[locale]/cafe/[slug]/page.tsx`)
- **إعادة الترتيب**: Menu → Payments → Social & Contact → Website → WiFi
- **SVG لوجوهات حقيقية**:
  - Vodafone Cash: SVG أيقونة حمراء (شكل Vodafone الرسمي — دائرة حمراء + علامة استفهام بيضاء)
  - InstaPay: SVG أيقونة خضراء (مستطيل دائري أخضر + سهم و نقطة)
- **Social Icons**: من دوائر صغيرة (12x12) إلى **2-column grid زراير مستطيلة** بنفس تصميم باقي العناصر
- **إزالة استيراد** `Smartphone` و `CreditCard` من lucide-react (لم نعد نحتاجهم)

### 6. التحقق من Image Optimization (`/_next/image`)
- الإعدادات الحالية في `next.config.ts` صحيحة (`res.cloudinary.com` في `remotePatterns`)
- اختبرنا المحلي ورجع 200

### 7. أمراض Production (gotap.vercel.app)
- أيقونات `/ar/icon` و `/ar/apple-icon` ما زالت 404 في Production
- **الحل**: يحتاج `git push` + Vercel deploy لأخذ الملفات الجديدة من `src/app/[locale]`

## الملفات المعدلة/الجديدة
| الملف | التغيير |
|---|---|
| `src/app/[locale]/icon.tsx` | **جديد** — أيقونة لـ `/[locale]/icon` |
| `src/app/[locale]/apple-icon.tsx` | **جديد** — أيقونة لـ `/[locale]/apple-icon` |
| `src/app/api/upload/route.ts` | إزالة `fetch_format` + إضافة stream error handler |
| `src/components/admin/CafeForm.tsx` | تحسين `res.json()` لقراءة non-JSON responses |
| `src/components/admin/ProductForm.tsx` | نفس التحسين |
| `src/components/profiles/ProfileWrapper.tsx` | إعادة ترتيب social links (Insta+TikTok / FB+Snap / Call+WA) |
| `src/app/[locale]/cafe/[slug]/page.tsx` | إعادة ترتيب + SVG أيقونات دفع + social grid |

## حالة البناء
- ✅ `npx tsc --noEmit` — **ناجح** (0 errors)
- ⚠️ `npx next build` — يفشل بسبب `next/font` (Google Fonts CDN غير متاح في البيئة المحلية)
- ✅ جميع الـ endpoints على `localhost:3000` — **200 OK**

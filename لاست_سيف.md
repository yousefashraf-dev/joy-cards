# لاست سيف — جلسة التطوير

**التاريخ:** 11 يوليو 2026  
**الفرع:** production-release  
**آخر commit:** ad08491

---

## التعديلات الأخيرة — Café Page: English-only + حقول جديدة

### 1. زرار المنيو → "Menu" إنجليزي ثابت
- **الملف:** `src/app/[locale]/cafe/[slug]/page.tsx`
- استبدال `{t("browseMenu")}` في السطرين 344 و 359 بـ `"Menu"` هاردكود

### 2. توزيع السوشيال ميديا الذكي
- **زوجي:** `grid-cols-2` (كل صف 2)
- **فردي ≥ 5:** آخر صف 3 عشان محدش يفضل لوحده
- **فردي 3:** `grid-cols-3` صف واحد
- **فردي 1:** `grid-cols-1` في النص

### 3. طرق الدفع — سنتر لو واحد
- لو في الاتنين (Vodafone + InstaPay): `grid-cols-2`
- لو في واحد بس: `max-w-xs mx-auto` في النص

### 4. هاردكود كل النصوص لإنجليزي
- إزالة كل استدعاءات `t()` من صفحة الكافيه
- النصوص الثابتة: `Payment Methods`, `Pay via Vodafone Cash`, `Pay via InstaPay`, `Our Website`, `Connect to Wi-Fi`, `Wi-Fi Network`, `Network Name`, `Password`, `Copied!`
- إزالة `useTranslations` import

### 5. Go Tap في الفوتر ← لينك
- **الملف:** `src/app/[locale]/cafe/[slug]/page.tsx`
- "Powered by **Go Tap**" بقت رابط `https://gotap.vercel.app/ar` (target=_blank)

### 6. حقول جديدة في فورم الكافيه

| الحقل | مكانه في الفورم | مكانه في صفحة العميل |
|---|---|---|
| **YouTube** | بعد الـ Social Links | 🎬 **دايرة مع السوشيال ميديا** |
| **Email** | مع الـ Social Links | ✉️ **دايرة مع السوشيال ميديا** |
| **Bio / Description** | بعد YouTube تحت الاسم | تحت اسم المكان دايمًا |
| **Working Hours** | بعد الـ Bio | 🕐 زرار → مودال بـ **12h format** |

### 7. Working Hours — Closed Toggle
- كل row فيه **Closed checkbox**
- لو متفعّل → الـ time inputs يختفوا
- في صفحة العميل: اليوم الـ Closed يظهر باللون الأحمر
- الوقت يظهر بـ 12h (9:00 AM — 11:00 PM)

### 8. Share Button
- أيقونة صغيرة تحت الفوتر
- تستخدم `navigator.share()` لو متاح، fallback نسخ الرابط

### 9. ترتيب السوشيال ميديا النهائي
```
[Facebook] [Instagram] [TikTok] [Snapchat]
[YouTube]  [Telegram]  [Email]  [WhatsApp]
[Google Maps] [Google Reviews] [Phone]
```

### 10. Fix: Email missing from getCafeBySlug
- **الملف:** `src/lib/cafe-schema.ts`
- إضافة `email: data.email` الناقصة في `getCafeBySlug`

---

## الملفات المعدلة

| الملف | التغيير |
|---|---|
| `src/lib/cafe-schema.ts` | إضافة `email`, `youtubeUrl`, `bio`, `workingHours` للـ interface + deserialization |
| `src/components/admin/CafeForm.tsx` | إضافة YouTube input, Bio textarea, Email input, Working Hours مع Closed toggle |
| `src/app/[locale]/cafe/[slug]/page.tsx` | English-only, Menu hardcoded, smart grid, YouTube/Email circles, Hours modal, Share button |
| `src/messages/ar.json` | إضافة ترجمات `email`, `bio`, `bioPlaceholder`, `workingHours`, `addHour` |
| `src/messages/en.json` | إضافة ترجمات `email`, `bio`, `bioPlaceholder`, `workingHours`, `addHour` |

---

## Commits

```
a3b7422 fix: cafe page smart social grid (even=2, odd=3), Menu button hardcoded, payment single centered
d92a9bd fix: hardcode cafe page to English-only + make Go Tap footer clickable
64fc7f5 feat: add youtubeUrl, bio, workingHours fields to Cafe + share button on cafe page
364fd22 feat: add email field, YouTube+Email as social circles, Closed toggle in working hours, 12h display
ad08491 fix: add missing email deserialization in getCafeBySlug
```

---

## حالة البناء
- ✅ TypeScript (`npx tsc --noEmit`) — 0 errors
- ✅ Dev server (`localhost:3000`) — 200 OK
- ✅ Vercel auto-deploy من `production-release`

---

## ملاحظات
- صفحة الكافيه **100% إنجليزي** (بغض النظر عن locale)
- Go Tap footer → `https://gotap.vercel.app/ar`
- الشحن: القاهرة والمحافظات 50 ج.م، الصعيد والمدن الساحلية 70 ج.م
- باسورد الأدمن: `oreo2552000`

---

## 1. إعادة تصميم صفحة الكافيه (Cafe Profile) كـ Premium Digital Card

### نقل الصفحة خارج الـ Layout الرئيسي
- **من:** `src/app/[locale]/(main)/cafe/[slug]/page.tsx`
- **إلى:** `src/app/[locale]/cafe/[slug]/page.tsx`
- تم حذف المجلد `(main)/cafe/` بعد النقل
- الصفحة أصبحت خارج route group `(main)` فلا ترث Navbar/Footer
- نفس pattern صفحة `/user/[id]`

### إضافة خط Dancing Script
- **الملف:** `src/app/[locale]/layout.tsx`
- استيراد `Dancing_Script` من `next/font/google` مع `variable: "--font-signature"`
- إضافته إلى `className` في `<html>`

### التصميم الجديد (Navy Blue & Shiny Gold + Glassmorphism)
- خلفية: `bg-gradient-to-b from-navy via-navy to-navy-dark`
- ألوان أساسية: `text-gold` مع `drop-shadow-[0_0_10px_rgba(212,175,55,0.5)]`
- Glassmorphism: `bg-white/5 backdrop-blur-md border border-gold/30`
- صورة بروفايل: دائرة `w-28 h-28` مع `ring-2 ring-gold` + توهج ذهبي
- اسم البراند: بخط `var(--font-signature)` (Dancing Script)

### مودال المنيو (Menu Modal)
- **جديد كلياً:** `MenuModal` component داخل الـ page
- زر "View Our Menu" بتصميم جلاسي → يفتح مودال
- المودال يستخدم `AnimatePresence` من framer-motion
- الصور بتظهر بـ CSS Scroll Snap (`flex overflow-x-auto snap-x snap-mandatory`)
- المستخدم يقدر يقلب الصور بالسحب (Swipe/Touch Gestures)
- نقاط دوت (indicators) تحت الشريط
- زر [X] لإغلاق المودال
- يدعم `menuType === "images"` و `"pdf"` و `"link"`

### أزرار التواصل (2-Column Grid)
- `grid grid-cols-2 gap-4`
- كل بطاقة بتصميم جلاسي: `bg-white/5 backdrop-blur-md border border-gold/30`
- `hover:bg-white/10 hover:border-gold/70 hover:shadow-[0_0_25px_rgba(212,175,55,0.25)]`
- أيقونات السوشيال في دوائر: `w-12 h-12 rounded-full bg-white/5 border border-gold/20`

### مودال الواي فاي
- باقي لكن بتحسين: `bg-navy/95 backdrop-blur-xl border border-gold/30`

### إضافة ترجمة جديدة
- `viewMenu`: "View Our Menu" (EN) / "عرض المنيو" (AR)

---

## 2. إصلاح مشكلة رفع الصور على In-App Browsers

### المشكلة
- في متصفحات Instagram/Facebook المدمجة، الضغط على منطقة رفع الصور ما يفتحش معرض الصور.

### الملفات المعدلة (4 files)

#### `LogoUpload.tsx` — تغيير جذري
- تحويل `<input>` من `opacity-0 z-10 overlay` إلى `className="hidden"` مع `useRef`
- الـ `<div>` الخارجي: `onClick` يستدعي `inputRef.current?.click()` مع `preventDefault` + `stopPropagation`

#### `AutoTapForm.tsx` — نفس التغيير
- إضافة `logoInputRef = useRef<HTMLInputElement>(null)`
- تحويل `<input>` من `opacity-0` إلى `hidden` + `onClick` على الـ `<div>`

#### `CafeForm.tsx` — 3 أزرار رفع
- إضافة `onClick={(e) => e.stopPropagation()}` على كل `<label>`
- تغيير `accept=".pdf"` → `accept="application/pdf"` (لمنع تعليق الموبايل)

#### `FormA_DigitalCards.tsx`
- إضافة `onClick={(e) => e.stopPropagation()}` على `<label>` (الـ `pdfRef` موجود بالفعل)

---

## 3. إصلاح صورة مكسورة في الصفحة الرئيسية

### المشكلة
- صورة `caffe.png` مش موجودة في `public/` والصفحة الرئيسية كانت بتظهر broken image
- الـ alt: "GoTap NFC sticker on a marble table next to a smartphone displaying a digital menu"

### التعديل
- **Rename:** `public/nfc cafe.png` → `public/nfc-cafe.png`
- **Edit:** `CafeShowcaseSection.tsx:39` — `src="/caffe.png"` → `src="/nfc-cafe.png"`

---

## ملفات تم إنشاؤها

| الملف | الوصف |
|-------|-------|
| `src/app/[locale]/cafe/[slug]/page.tsx` | صفحة الكافيه المعاد تصميمها |
| `public/nfc-cafe.png` | صورة الكافيه الجديدة |

## ملفات تم تعديلها

| الملف | التعديل |
|-------|---------|
| `src/app/[locale]/layout.tsx` | إضافة Dancing Script font |
| `src/messages/en.json` | إضافة `viewMenu` |
| `src/messages/ar.json` | إضافة `viewMenu` |
| `src/components/forms/LogoUpload.tsx` | Fix upload on in-app browsers |
| `src/components/forms/AutoTapForm.tsx` | Fix upload on in-app browsers |
| `src/components/admin/CafeForm.tsx` | Fix upload + change PDF accept |
| `src/components/forms/FormA_DigitalCards.tsx` | Fix upload on in-app browsers |
| `src/components/home/CafeShowcaseSection.tsx` | Fix broken image src |

## Commits

```
5329d8d feat: redesign cafe profile as standalone premium digital card
3a7380a fix: mobile file upload not working in in-app browsers
ebd66e5 fix: broken cafe showcase image on homepage
```

---

# خطة التطوير القادمة — 3 محاور رئيسية

**بتاريخ:** 23 يونيو 2026  
**الحالة:** 📋 مخططة — لم تنفذ بعد  
**صور المتوفرة في public:** `/2.jpeg` (6سم), `/1.jpeg` (25سم), `/3.jpeg` (35سم)

---

## المحور الأول: دليل المقاسات البصري (Visual Size Guide) + تبسيط الأسعار

### الهدف
استبدال Step 4 في AutoTapForm (input نصي + SVG car diagram + نصوص طويلة) بـ 3 أزرار مقاسات مع Preview Card بصري + سعر موحد.

### تفاصيل التنفيذ

#### 1. تغيير `src/lib/types.ts`
- تغيير `logoWidthCm` من `string` إلى `"6" | "25" | "35"` في `AutoTapFields`

#### 2. إعادة كتابة Step 4 في `src/components/forms/AutoTapForm.tsx`
**إزالة:**
- `<input type="text">` للـ logoWidthCm
- SVG car window diagram بالكامل
- النصوص الطويلة (`t("fields.windowStandard")`, `t("helper.logoWidth")`, إلخ)
- textarea الملاحظات (Order Notes)

**إضافة:**
```tsx
{/* Size buttons */}
<div className="grid grid-cols-3 gap-3 mb-6">
  {sizes.map((size) => (
    <button
      key={size.value}
      onClick={() => handleFieldChange("logoWidthCm", size.value)}
      className={`flex flex-col items-center gap-1 p-4 rounded-2xl border transition-all duration-300 ${
        data.logoWidthCm === size.value
          ? "border-gold bg-gold/10 text-gold"
          : "border-white/10 bg-dark-card/50 text-slate-body hover:border-white/30"
      }`}
    >
      <span className="text-2xl font-bold">{size.label}</span>
      <span className="text-xs text-slate-muted">{size.desc}</span>
    </button>
  ))}
</div>

{/* Image Preview with AnimatePresence fade-in */}
{data.logoWidthCm && (
  <AnimatePresence mode="wait">
    <motion.div
      key={data.logoWidthCm}
      initial={{ opacity: 0, y: 10 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, y: -10 }}
      transition={{ duration: 0.3 }}
    >
      <Image
        src={sizeImages[data.logoWidthCm]}
        alt={`Sticker size ${data.logoWidthCm}cm`}
        width={600} height={400}
        className="w-full h-auto rounded-2xl object-cover"
        unoptimized
      />
      <p className="text-center text-sm text-gold mt-2">
        مثال توضيحي لمقاس {data.logoWidthCm} سم على السيارة
      </p>
    </motion.div>
  </AnimatePresence>
)}

{/* Price */}
<div className="text-center mt-6 p-4 rounded-2xl bg-white/5 backdrop-blur-md border border-gold/30">
  <span className="text-gold font-bold text-lg">200 ج.م + مصاريف الشحن</span>
</div>
```

**خريطة الصور:**
```ts
const sizeImages: Record<string, string> = {
  "6": "/2.jpeg",
  "25": "/1.jpeg",
  "35": "/3.jpeg",
};
```

#### 3. إزالة النصوص الطويلة من كل Steps
- Step 2 (Theme): إزالة `t("helper.previewGuide")`, `t("helper.themeHeader")`
- Step 3 (Logo): إزالة `t("logoText")` و `t("uploadHelper")`
- Step 1 (Personal info): إزالة أي نصوص مساعدة طويلة
- الحفاظ فقط على Label المطلوب للـ Input

#### 4. تحديث `src/lib/pricing.ts`
```ts
export function calcBasePrice(): number {
  return 200; // سعر موحد للاستيكر
}
```
- إزالة `profileType` parameter
- `calcTotal` يبقى `{ basePrice: 200, shippingFee }` (الشحن حسب المنطقة)

#### 5. تحديث `src/lib/submitOrder.ts`
- `buildProductDetails`: تحديث لعرض المقاس الجديد (6/25/35) بدلاً من `logoWidthCm` النصي

#### 6. تحديث `src/messages/ar.json` و `en.json`
- إزالة ترجمات النصوص الطويلة
- إضافة ترجمات لأزرار المقاسات ووصف الصورة

### الملفات المتأثرة (6):

| الملف | نوع التغيير |
|---|---|
| `src/components/forms/AutoTapForm.tsx` | تعديل — Step 4 + إزالة نصوص |
| `src/lib/types.ts` | تعديل — نوع `logoWidthCm` |
| `src/lib/pricing.ts` | تعديل — سعر موحد 200 |
| `src/lib/submitOrder.ts` | تعديل — تفاصيل المنتج |
| `src/messages/ar.json` | تعديل — ترجمات |
| `src/messages/en.json` | تعديل — ترجمات |

---

## المحور الثاني: لوحة تحكم المنتجات (CRUD)

### الهدف
إضافة تبويب "إدارة المنتجات" في Dashboard، بنفس Pattern Café Form/Cafe Table، مربوط بـ Firestore collection `products`.

### التفاصيل

#### 1. ملف جديد: `src/lib/product-schema.ts`

```ts
export interface Product {
  id?: string;
  name: string;
  price: number;
  description: string;
  images: string[];
  createdAt?: number;
}

// دوال CRUD مطابقة تماماً لـ cafe-schema.ts:
export async function addProduct(data: Omit<Product, "id" | "createdAt">) { ... }
export async function updateProduct(id: string, data: Partial<Product>) { ... }
export async function deleteProduct(id: string) { ... }
export async function getAllProducts(): Promise<Product[]> { ... }
```

- Collection: `products`
- `addDoc(collection(db, "products"), { ...data, createdAt: serverTimestamp() })`
- رفع الصور: عبر `/api/upload` (Cloudinary) — نفس pattern CafeForm

#### 2. ملف جديد: `src/components/admin/ProductForm.tsx`
- **نفس كلاسات الـ Inputs الموجودة في CafeForm:**
  - `inputClass`: `w-full px-4 py-2.5 rounded-lg glass bg-dark-card border border-white/10 text-sm text-slate-light ...`
  - `labelClass`: `block text-sm text-slate-muted mb-1`
- **الحقول:**
  - اسم المنتج (text input)
  - السعر (number input)
  - الوصف (textarea)
  - رفع الصور (multiple images via Cloudinary)
- **Buttons:** حفظ / إلغاء
- **Props:** `product?: Product | null`, `onSuccess`, `onCancel`

#### 3. ملف جديد: `src/components/admin/ProductTable.tsx`
- **نفس كلاسات CafeTable:**
  - `bg-matte-card/50 border border-white/5 rounded-xl overflow-hidden`
  - `text-slate-muted text-xs uppercase tracking-wider`
- **الأعمدة:** صورة مصغرة | الاسم | السعر | تحكم (Edit / Delete)
- **Props:** `products: Product[]`, `onEdit`, `onRefresh`

#### 4. تعديل: `src/app/[locale]/admin/dashboard/page.tsx`
- إضافة `Tabs` (كافيهات / منتجات) أعلى الصفحة
- كل tab يشغل الـ Component الخاص به
- نفس نمط الأمان (password `oreo2552000`)

```tsx
const [activeTab, setActiveTab] = useState<"cafes" | "products">("cafes");

// Tabs UI
<div className="flex gap-2 mb-6">
  <button onClick={() => setActiveTab("cafes")} 
          className={`px-6 py-2.5 rounded-xl text-sm font-medium transition-all ${
            activeTab === "cafes" ? "bg-gold/20 text-gold border border-gold/40" : "bg-white/5 text-slate-muted border border-white/10"
          }`}>
    الكافيهات
  </button>
  <button onClick={() => setActiveTab("products")}
          className={`px-6 py-2.5 rounded-xl text-sm font-medium transition-all ${
            activeTab === "products" ? "bg-gold/20 text-gold border border-gold/40" : "bg-white/5 text-slate-muted border border-white/10"
          }`}>
    المنتجات
  </button>
</div>
```

### الملفات (4):

| الملف | نوع التغيير |
|---|---|
| `src/lib/product-schema.ts` | **جديد** — Product CRUD |
| `src/components/admin/ProductForm.tsx` | **جديد** — Product form |
| `src/components/admin/ProductTable.tsx` | **جديد** — Product table |
| `src/app/[locale]/admin/dashboard/page.tsx` | تعديل — إضافة Tabs |

---

## المحور الثالث: واجهة المتجر (Shop UI) في الصفحة الرئيسية

### الهدف
عرض المنتجات من Firestore في Grid بتصميم Glassmorphism + مودال تفاصيل + طلب عبر واتساب.

### التفاصيل

#### 1. ملف جديد: `src/components/shop/ShopSection.tsx`
- **Location:** يضاف بين `<ProductsGrid />` و `<FaqAccordion />` في الصفحة الرئيسية
- **Functionality:** Fetch من `getAllProducts()` + عرض Grid
- **Headline:** "تسوق منتجاتنا" / "Our Products"

```tsx
export default function ShopSection() {
  const [products, setProducts] = useState<Product[]>([]);
  const [selected, setSelected] = useState<Product | null>(null);

  useEffect(() => {
    getAllProducts().then(setProducts).catch(console.error);
  }, []);

  if (products.length === 0) return null;

  return (
    <section className="py-20 lg:py-28">
      <div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 className="text-3xl font-bold text-slate-light mb-10 text-center">
          {t("shopTitle")}
        </h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {products.map((product) => (
            <ProductCard key={product.id} product={product} onClick={setSelected} />
          ))}
        </div>
      </div>

      <AnimatePresence>
        {selected && <ProductDetailModal product={selected} onClose={() => setSelected(null)} />}
      </AnimatePresence>
    </section>
  );
}
```

#### 2. ملف جديد: `src/components/shop/ProductCard.tsx`
```tsx
// كلاسات التصميم:
className="bg-white/5 backdrop-blur-md border border-gold/30 rounded-2xl overflow-hidden
           hover:bg-white/10 hover:border-gold/70 hover:shadow-[0_0_25px_rgba(212,175,55,0.25)]
           transition-all duration-300 group cursor-pointer"

// Image: object-cover مع hover scale
// Name: text-slate-light text-lg font-semibold
// Price: text-gold font-bold text-xl
```

#### 3. ملف جديد: `src/components/shop/ProductDetailModal.tsx`
```tsx
// Modal design (نفس pattern WifiModal):
className="fixed inset-0 bg-black/80 backdrop-blur-xl flex items-center justify-center p-4 z-50"

// Content box:
className="w-full max-w-lg bg-navy/95 backdrop-blur-xl border border-gold/30 rounded-2xl
           overflow-hidden shadow-[0_0_50px_rgba(212,175,55,0.2)]"

// Counter [+]/[-]:
<button onClick={() => setQty(Math.max(1, qty - 1))} className="w-10 h-10 rounded-xl bg-white/5 ...">-</button>
<span className="text-xl font-bold text-gold min-w-[40px] text-center">{qty}</span>
<button onClick={() => setQty(qty + 1)} className="w-10 h-10 rounded-xl bg-white/5 ...">+</button>

// WhatsApp order button:
<button onClick={() => {
  const msg = `مساء الخير، حابب أطلب منتج: ${product.name} - عدد: ${qty} قطعة.`;
  const url = `https://wa.me/201585599678?text=${encodeURIComponent(msg)}`;
  window.open(url, "_blank", "noopener,noreferrer");
}}
  className="w-full py-4 rounded-2xl bg-white/10 backdrop-blur-md border border-gold/50
             text-gold font-bold text-lg hover:bg-white/20 ...">
  ا طلب الآن عبر واتساب
</button>
```

#### 4. تعديل: `src/app/[locale]/(main)/page.tsx`
```tsx
import ShopSection from "@/components/shop/ShopSection";

export default function HomePage() {
  return (
    <>
      <HeroSection />
      <LiveStatsCounter />
      <HowItWorksSection />
      <CafeShowcaseSection />
      <ReviewGallery />
      <ProductsGrid />
      <ShopSection />          {/* <-- NEW */}
      <FaqAccordion />
    </>
  );
}
```

#### 5. تعديل: `src/lib/constants.ts`
- إضافة أو إعادة استخدام `WHATSAPP_NUMBER` الموجود (201558599678)

#### 6. تعديل: `src/messages/*.json`
- إضافة ترجمات: `shopTitle`, `orderNow`, `piece`, `totalOrder`, `whatsappShopMessage`

### الملفات (6):

| الملف | نوع التغيير |
|---|---|
| `src/components/shop/ShopSection.tsx` | **جديد** |
| `src/components/shop/ProductCard.tsx` | **جديد** |
| `src/components/shop/ProductDetailModal.tsx` | **جديد** |
| `src/app/[locale]/(main)/page.tsx` | تعديل — إضافة ShopSection |
| `src/lib/constants.ts` | تعديل — ثابت واتساب |
| `src/messages/ar.json` + `en.json` | تعديل — ترجمات |

---

## ملخص جميع الملفات (18 ملف)

| المحور | ملفات جديدة | ملفات معدلة | المجموع |
|---|---|---|---|
| 1. Size Guide + Pricing | 0 | 6 | 6 |
| 2. Admin Products CRUD | 3 | 1 | 4 |
| 3. Shop UI | 3 | 3 | 6 |
| **الإجمالي** | **6 ملفات جديدة** | **10 تعديلات** | **16** |

### الملفات الجديدة:
```
src/lib/product-schema.ts
src/components/admin/ProductForm.tsx
src/components/admin/ProductTable.tsx
src/components/shop/ShopSection.tsx
src/components/shop/ProductCard.tsx
src/components/shop/ProductDetailModal.tsx
```

### الملفات المعدلة:
```
src/components/forms/AutoTapForm.tsx
src/lib/types.ts
src/lib/pricing.ts
src/lib/submitOrder.ts
src/app/[locale]/admin/dashboard/page.tsx
src/app/[locale]/(main)/page.tsx
src/lib/constants.ts
src/messages/ar.json
src/messages/en.json
```

---

## 7️⃣ Session 5 — إعادة هيكلة شاملة للموقع (Refactoring & New Features)

**التاريخ:** 25 يونيو 2026  
**الهدف:** تنفيذ خطة إعادة هيكلة وتطوير شاملة: تحديث الثيمات، Hero Motion، Navbar، Shop سكشن، Review Carousel، Size Guide، Dashboard CRUD، ونظام خصومات.

---

### التغييرات الرئيسية

#### 1. الثيمات — 5 ثيمات جديدة (9 في Selector)
- **5 جديدة:** Energy Power (Red Bull), Game of Thrones, Stitch, Batreq, Pokemon Yellow
- **2 قديمة:** Batman, Spider-Man
- **2 قديمة preserved:** Cyber Cyan Drift, Terminal Dark Glow
- **5 ثيمات قديمة مخفية من Selector:** energy-lightning, neon-red-track, auto-nardo-stealth, classic-royal-silver, liquid-aurora
- **ألوان مخصصة لكل ثيم** مستوحاة من شخصيته (Gradients في AutoTapPreview)
- **ملفات:** `src/lib/types.ts`, `src/lib/constants.ts`, `src/components/forms/AutoTapPreview.tsx`, `src/components/profiles/ProfileWrapper.tsx`

#### 2. التسعير — سعر موحد
- `calcBasePrice()` يرجع 200 ثابت (إزالة اعتماد profileType)
- `calcTotal()` تأخذ address فقط
- **ملف:** `src/lib/pricing.ts`

#### 3. Size Guide (Step 4 في AutoTapForm)
- 3 أزرار مقاس: 6 سم، 25 سم، 35 سم
- Preview Image بـ AnimatePresence (fade-in 300ms)
- صور المقاسات: 6cm → `/2.jpeg`، 25cm → `/1.jpeg`، 35cm → `/3.jpeg`
- `logoWidthCm` type: `"6" | "25" | "35" | ""`
- إزالة 11+ نصاً طويلاً ونصوص مساعدة
- **ملف:** `src/components/forms/AutoTapForm.tsx`

#### 4. Dashboard CRUD للمنتجات
- **product-schema.ts** (جديد): CRUD لـ `products` collection في Firestore، حقل `salePrice?`
- **ProductForm.tsx** (جديد): Two-column layout (left form, right images grid)، Cloudinary upload
- **ProductTable.tsx** (جديد): Grid كروت + حقل خصم per product + salePrice
- **dashboard/page.tsx**: Side Navigation (كافيهات/متجر) + Two-Column Layout
- **مجلد:** `src/components/admin/`

#### 5. Hero Motion التفاعلي
- **HeroMotion.tsx** (جديد): NFC دائرة ذهبية + Smartphone SVG + Framer Motion (Slide & Zoom → Touch → Popup "المنيو الرقمي")
- **HeroSection.tsx**: أصبح wrapper بسيط يستخدم HeroMotion
- **ملف:** `src/components/home/HeroMotion.tsx`

#### 6. Navbar — 4 روابط
- الرئيسية، تاب السيارات، المطاعم والكافيهات، متجر الميداليات والإكسسوارات
- **ملف:** `src/components/layout/Navbar.tsx`

#### 7. Review Gallery — Infinite Moving Carousel
- معاد بالكامل: Framer Motion (repeat: Infinity)
- **ملف:** `src/components/home/ReviewGallery.tsx`

#### 8. Shop سكشن
- **ShopProducts.tsx** (جديد): سكشن "تصفح منتجاتنا" — Fetch أول 3 منتجات، Horizontal Scroll Snap، زر "تصفح المتجر بالكامل"
- **/shop/page.tsx** (جديد): صفحة متجر كاملة مع Product Detail Modal (+/- كمية + WhatsApp Order)
- **main/page.tsx**: إضافة ShopProducts بعد Hero
- **مجلد:** `src/components/shop/`, `src/app/[locale]/(main)/shop/`

#### 9. تحديث submitOrder.ts
- `buildProductDetails`: logoWidthCm → المقاس (size)
- إزالة pricingInfo من الرسائل
- **ملف:** `src/lib/submitOrder.ts`

#### 10. ترجمات (messages)
- **en.json + ar.json**: إضافة `nav.shop`, `hero.tagline`, `shop.*` (10 keys), `products.autoTap.*` (title, desc, badge, features, shippingLabel), `admin.*` (cafesTab, productsTab, addProduct, editProduct, salePrice, applyDiscount, removeDiscount, specialOffer, form.productName/price/category/description/images), `forms.section.size`, `sizeCm`, `sizeExample`, `shippingLabel`
- **إزالة:** `helper.*`, `logoText`, `pricingInfo`

---

### الملفات المنشأة حديثاً
```
src/lib/product-schema.ts
src/components/admin/ProductForm.tsx
src/components/admin/ProductTable.tsx
src/components/home/HeroMotion.tsx
src/components/shop/ShopProducts.tsx
src/components/shop/ProductDetailModal.tsx
src/app/[locale]/(main)/shop/page.tsx
```

### الملفات المعدلة
```
src/lib/types.ts
src/lib/constants.ts
src/lib/pricing.ts
src/lib/submitOrder.ts
src/components/forms/AutoTapForm.tsx
src/components/forms/AutoTapPreview.tsx
src/components/profiles/ProfileWrapper.tsx
src/components/home/HeroSection.tsx
src/components/layout/Navbar.tsx
src/components/home/ReviewGallery.tsx
src/app/[locale]/admin/dashboard/page.tsx
src/app/[locale]/(main)/page.tsx
src/app/[locale]/(main)/auto-tap/page.tsx
src/messages/en.json
src/messages/ar.json
```

### حالة الاختبار
- ✅ Dev server (Next.js 16.2.9, Turbopack) — شغال على `localhost:3000`
- ✅ `/en` ← 200
- ✅ `/en/auto-tap` ← 200
- ✅ `/en/shop` ← 200
- ✅ `/en/admin/dashboard` ← 200
- ✅ No MISSING_MESSAGE errors
- ✅ JSON صحيح (validated)

### ملاحظات
- `yellow_-preview.png` غير موجود — الموجود هو `yellow_-removebg-preview.png`
- `text-gradient-gold` utility موجودة مسبقاً في globals.css
- `products.autoTap.*` keys أُضيفت لـ SuccessModal (title, desc...)
- `form.save` generic (ليس خاص بالكافيه أو المنتج)

---

## 8️⃣ Session 9 — إعادة بناء Form تاب السيارات (مسارين)

**التاريخ:** 01 يوليو 2026  
**الهدف:** إعادة بناء Form تاب السيارات بمسارين (أيقونة/يوزر) بخطوات واضحة وتصميم لاكشري.

### ملخص التغييرات

#### 1. مسارين متوازيين (Two Funnels)
- **الأيقونة** (100 ج.م): stickerType → platform → shipping → customization → theme → review
- **اليوزر** (150 ج.م): stickerType → shipping → customization → theme → sizing → review
- الـ `currentSteps` بتتغير حسب النوع (sizing للأيقونة/ platform لليوزر)
- الـ step indicator بيظهر الخطوات حسب المسار المختار

#### 2. تصميم كروت stickerType الجديد
- **أيقونة**: 3 أيقونات (إنستا + تيك توك + سناب) + علامة `+` + "مقاس 5×5 سم" + السعر
- **يوزر**: @username في badge + "مقاس مخصص" + السعر
- كل كارت فيه badge السعر والمقاس جنب بعض

#### 3. خطوة اختيار المنصة (جديدة)
- 3 أزرار مصغرة `p-3` بدل الكروت الكبيرة
- صورة `w-10 h-10` + أيقونة + اسم المنصة
- فل ذهبي مع علامة `✓` عند الاختيار

#### 4. التسعير
- `calcBasePrice(stickerType)` → 100 للأيقونة / 150 لليوزر (كان 200 ثابت)
- `calcTotal(stickerType, address)` — يأخذ نوع الاستيكر كبارامتر

#### 5. خطوة التخصيص (Customization)
- **Display Name** + live preview الدائري
- **6 حقول سوشيال ميديا** في grid 3×2 (إنستا، تيك توك، سناب، فيسبوك، واتساب، تليفون)
- **رفع اللوجو** بشكل دائري جديد `w-28 h-28` بحد ذهبي متقطع + أيقونة Upload

#### 6. حقل الملاحظات
- أُزيل من shipping و customization
- أُضيف في خطوة المقاس (sizing) — لليوزر فقط

#### 7. Manual Size Input (جديد)
- تحت أزرار 6/25/35 فيه input يدوي لمقاس مخصص

#### 8. اللينكات تظهر في الثيم
- AutoTapPreview يقرأ `data.socialLinks` كلها (6 بلاتفورم)
- يستخدم `formatSocialLink` لتظبيط الرابط (يقبل username/URL/رقم)

#### 9. خلفية الثيم (Scatter)
- إضافة collision detection: مسافة لا تقل عن 14% بين العناصر
- يحاول 30 مرة عشان يلاقي موقع بعيد

#### 10. صور جديدة
- `public/instgram.png` — صورة إنستجرام
- `public/tik tok.png` — صورة تيك توك
- `public/snap chat.png` — صورة سناب شات

### الملفات المعدلة

| الملف | التغيير |
|-------|---------|
| `src/lib/types.ts` | إضافة `selectedPlatform` لـ `AutoTapFormData` و `AutoTapFields` |
| `src/lib/pricing.ts` | `calcBasePrice(stickerType)` → 100/150 |
| `src/components/forms/AutoTapForm.tsx` | **إعادة كتابة** — 7 steps, مسارين, platform step, 6 social links, manual size, logo design جديد |
| `src/components/forms/AutoTapPreview.tsx` | قراءة socialLinks كلها + `formatSocialLink` |
| `src/lib/scatterBackground.ts` | Collision detection (minDist=14, maxAttempts=30) |
| `src/lib/submitOrder.ts` | إضافة `selectedPlatform` للـ payload |
| `src/app/[locale]/(main)/auto-tap/page.tsx` | INITIAL_DATA + dynamic pricing bar |
| `src/messages/ar.json` | إضافة `steps.platform` و `section.platform` |
| `src/messages/en.json` | إضافة `steps.platform` و `section.platform` |

### حالة الاختبار
- ✅ Dev server — شغال على `localhost:3000`
- ✅ `/ar/auto-tap` ← 200
- ✅ `/en/auto-tap` ← 200
- ✅ TypeScript سليم (tsc --noEmit)
- ✅ No MISSING_MESSAGE errors
- ✅ JSON صحيح
- ⚠ `next build` عنده مشكلة مع Turbopack (مش موجودة قبل كدا، الديف سيرفر شغال)

### ملاحظات
- Build failure موجود في الكود الأصلي كمان — الديف سيرفر شغال 100%
- الشحن: القاهرة والمحافظات 50 ج.م، الصعيد والمدن الساحلية 70 ج.م
- رسالة الواتساب بتشمل: المنصة + المقاس + الاسم + العنوان + السعر + الشحن
```
